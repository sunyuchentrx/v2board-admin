# 开发环境默认值（vite dev 自动加载）。生产构建不读这个文件。
# 需要覆盖时新建 .env.development.local（已在 .gitignore 里），不要改本文件。
# 不要用 .env.local / .env：它们在 npm run build 时也会加载。

# 开发期的 secure_path。生产环境由后端注入的 window.settings.secure_path 决定，
# 不走这里。设成什么都行，只要和你访问时的 URL 前缀一致：
#   http://localhost:5173/{VITE_SECURE_PATH}/v2/login
VITE_SECURE_PATH=admin

# /api 的代理目标：开发期所有 /api 请求会被转发到这里。
# 改成你自己的后端地址，例如 https://your-panel.example.com
VITE_API_TARGET=http://127.0.0.1:8080
